
#include <iostream>
#include <vector>
using namespace std;
int main()
{
    int n, k, temp, output;
    output = -1;
    vector<long long> attraction;
    scanf("%d %d", &n, &k);
    for (int i = 0; i < n; i++)
    {
        scanf("%lld", &temp);
        attraction.push_back(temp);
    }

    for (int i = 0; i < n; i++)
    {
        for (int j = 1; j <= ; j++)
        {
            temp = 
        }
    }

    for (int i = 0; i < n; i++)
    {
        printf("%d", attraction[i]);
    }
    return 0;
}
