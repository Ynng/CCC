#include <iostream>
#include <vector>
using namespace std;
int main()
{
    int n, p, ridings[100005][15];
    scanf("%d", &n);
    scanf("%d", &p);
    for (int i = 1; i <= n; i++)
    {
        for (int j = 1; j <= p; j++)
        {
            scanf("%d", &ridings[i][j]);
        }
    }
    for (int i = 1; i <= n; i++)
    {
            
    }
}